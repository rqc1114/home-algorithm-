#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

int main(void)
{
	int w, h;
	int p, q;
	int t;
	int p1, q1;
	int w=1;

	scanf("%d %d", &w, &h);
	scanf("%d %d", &p, &q);
	scanf("%d", &t);


	for (int i = 0; i < t; i++)
	{
		p++, q++;

		if (p <= w || q <= h)
		{
			if ()  //세타 값에 따라
			{
				{
					w *= -1;
					p *= w;
					q *= w;
				}
			}

			else if ()
			{
				w *= -1;
				p *= w;
				q *= w;
			}

			else if ()
			{
				w *= -1;
				p *= w;
				q *= w;
			}
		}
		

		

		

	}



	return 0;
}
